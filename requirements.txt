# AI Pulse — runtime dependencies (Python 3.11+)
feedparser==6.0.11        # RSS/Atom parsing
httpx==0.27.2             # HTTP client with retry/timeout support
anthropic==0.39.0         # Official Anthropic SDK (Claude)
pydantic==2.9.2           # Config + data validation
PyYAML==6.0.2            # config.yaml loader
python-dotenv==1.0.1     # load .env for local runs
tenacity==9.0.0          # retry/backoff for network + API calls
tzdata==2024.2           # IANA tz database (Windows/CI have no system tzdata)
